/**
 * src/data/ai.ts
 *
 * /ai 页所需的结构化数据：我与 AI 协作的几条原则。
 * 不放置于 Content Collection，形态与 src/data/about.ts 一致（见 CONTENT_MODEL.md §2.8）。
 *
 * 内容来源（提炼转写，非照搬）：
 *   AGENTS.md §2 文档先行 / §2.4 取舍原则 / §4 工程纪律 / §1 隐私
 *   user rules（先澄清、改前征求同意、复用已有、最小改动）
 *   docs/content/WRITING_GUIDE.md §8 AI 披露
 *
 * 隐私自查（AGENTS.md §1 与 RFC-0004 §4.6）：
 *   ✅ 仅方法论层面的公开表达，无真实姓名 / 联系方式 / 公司内部细节
 *   ✅ 不照搬仓库内部宪法原文（隐私字段清单、治理细节）
 */

import { z } from "astro:content";

/* ============================================================
 * Schema
 * ============================================================ */

const principle = z.object({
  title: z.string(),
  body: z.string(),
});

const aiSchema = z.object({
  intro: z.string(),
  principles: z.array(principle).min(3).max(8),
  lastUpdated: z.coerce.date(),
});

/* ============================================================
 * Data
 * ============================================================ */

const aiData = {
  intro:
    "我怎么和 AI 一起干活——几条反复用到、也反复验证过的原则。它们让 AI 更像一个懂分寸的搭档，而不是一个急着写代码的工具。",

  principles: [
    {
      title: "文档先行",
      body: "先给 AI 一份「宪法」再开工。把约束、品味与边界写进文档，AI 才能稳定发挥——而不是每次从零猜我想要什么。",
    },
    {
      title: "先方案，后代码",
      body: "能讲清的就别让它猜。动手前先对齐方案、说明取舍、征求同意；想清楚的三十分钟，省下返工的三小时。",
    },
    {
      title: "少即是多",
      body: "拿不准要不要做时，默认不做。加法永远比减法容易，多余的功能会养出依赖——克制是品味的证据，不是缺陷。",
    },
    {
      title: "为三年后写",
      body: "选对未来更友好的方案，哪怕当下更费力。今天的省力，往往是明天的债。",
    },
    {
      title: "优雅胜过方便",
      body: "好的便利来自好的架构，而不是妥协的捷径。不为「快一点」牺牲整体的一致与干净。",
    },
    {
      title: "复用，而非新造",
      body: "先在已有的范式里找答案，用最小的改动解决问题。一致的系统比一个新点子更值钱。",
    },
    {
      title: "透明与边界",
      body: "隐私不可协商，敏感信息逐项确认；AI 深度参与的内容，如实标注。可信，才谈得上协作。",
    },
  ],

  lastUpdated: "2026-05-29",
};

/* ============================================================
 * 加载时校验（违反 schema 直接抛错，构建失败）
 * ============================================================ */
export const AI = aiSchema.parse(aiData);
export type Ai = z.infer<typeof aiSchema>;
